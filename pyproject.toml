[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "firebird-proc-usage"
version = "0.1.0"
description = "Low-overhead Firebird procedure usage collector"
requires-python = ">=3.9"

[project.scripts]
proc-usage = "proc_usage.cli:main"
